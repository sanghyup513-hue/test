MLPERF_GPU_LIST가 두 번 들어가 있습니다. 이건 제가 며칠 전 run_scaleout.sh에 넣었다가 실패해서 원복했던 패치의 잔재입니다. 원복이 덜 됐거나 중복 적용됐습니다.

bash
grep -n 'MLPERF_GPU_LIST' scaleout/run_scaleout.sh
grep -c "locals().get('gpu_ids'" code/llmlib/launch_server.py

MLPERF_GPU_LIST가 나오면 지워야 합니다. 405B가 4장에서 성공했던 시점에는 이게 없었습니다.

bash
cp scaleout/run_scaleout.sh /tmp/rs-$(date +%H%M).bak
sed -i 's/,MLPERF_GPU_LIST//g; s/; export MLPERF_GPU_LIST="${gpu_list}"//' scaleout/run_scaleout.sh
bash -n scaleout/run_scaleout.sh && echo "문법 OK"
grep -n 'MLPERF_GPU_LIST\|mpi_mode' scaleout/run_scaleout.sh

sed가 정확히 먹었는지 grep으로 확인하세요. 안 맞으면 백업에서 되돌릴 수 있습니다.
