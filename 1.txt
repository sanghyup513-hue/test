sudo modprobe ib_umad
lsmod | grep ib_umad
sudo systemctl start nvidia-fabricmanager
sleep 20
systemctl is-active nvidia-fabricmanager
nvidia-smi --format=csv --query-gpu=index,fabric.state,fabric.status

echo ib_umad | sudo tee /etc/modules-load.d/nvidia-fabricmanager.conf
cat /etc/modules-load.d/nvidia-fabricmanager.conf
